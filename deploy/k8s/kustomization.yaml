apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: teleport

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Uncomment to add common labels
# commonLabels:
#   app.kubernetes.io/name: teleport
#   app.kubernetes.io/instance: teleport-prod
