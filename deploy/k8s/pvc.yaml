apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teleport-data
  namespace: teleport
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Uncomment and set your storage class if needed
  # storageClassName: standard
